# Let the env know where ActiveMQ runs.

export BOXEN_ACTIVEMQ_PORT=<%= scope.lookupvar('activemq::config::port') %>
export BOXEN_ACTIVEMQ_URL="activemq://localhost:$BOXEN_ACTIVEMQ_PORT/"
export BOXEN_STOMP_PORT=<%= scope.lookupvar('activemq::config::stomp_port') %>
export BOXEN_STOMP_URL="activemq://localhost:$BOXEN_STOMP_PORT/"
